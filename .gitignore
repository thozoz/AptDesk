# IDE / Editor 
.idea/
.vscode/
.claude/
.gemini/
.planning
claude*
.serena
docs

# Build artifacts
docker/*.tar.gz
docker/*.sha256

# Android build outputs
*.apk
*.aar
build/
app/build/
.gradle/
local.properties
temp

# Frontend build output — regenerated by the Gradle buildFrontend task / `npm run build`.
# Runtime-only assets (www/bin/, www/libs/) stay tracked; only the Vite bundle is ignored.
app/src/main/assets/www/index.html
app/src/main/assets/www/assets/

# Signing secrets — never commit
keystore.properties
*.jks
*.keystore