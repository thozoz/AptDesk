# Copy this file to `keystore.properties` (untracked) to sign release builds.
# The real keystore.properties and any *.jks/*.keystore are gitignored — never commit secrets.
storeFile=/absolute/path/to/aptdesk-release.jks
storePassword=CHANGEME
keyAlias=aptdesk
keyPassword=CHANGEME
